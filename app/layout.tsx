import './globals.css'
export const metadata={title:'Prompt Optimizer — Turn ideas into clean prompts & JSON',description:'Optimize any prompt into a clean JSON and a reusable optimized prompt.',icons:[{rel:'icon',url:'/icon.svg'}]}
export default function RootLayout({children}:{children:React.ReactNode}){return(<html><body>{children}</body></html>)}
